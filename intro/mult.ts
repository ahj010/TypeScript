const mult = (a: number , b: number ) => {
    const result =  a *b
    console.log(`Multiplied ${a} amd ${b}, the result is ${result}`);
}

a : number = Number(process.argv[2])
b : number = Number(process.argv[3])
